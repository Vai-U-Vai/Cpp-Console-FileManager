#pragma once
#include<string>
using namespace std;

class Input {
public:
    static string mask();
    static string name(string param);
    static string path();
    static string  path(string s);
};